integrator               = steep
nsteps                   = 50000

nstenergy                = 500
nstlog                   = 500

define                   = -DFLEXIBLE

cutoff-scheme            = Verlet

coulombtype              = PME
rcoulomb                 = 1.0

vdwtype                  = Cut-off
rvdw                     = 1.0
DispCorr                 = EnerPres
